#include "Mediatheque.h"

int main() {
    Mediatheque app;
    //// ----- DEMANDER UTILISATEUR SI C EST UN ADMIN OU PAS,  ----- ////
    // If admin -> user = Administrateur();
    // Else user = Utilisateur();
    app.dispatch();
    return 0;
}